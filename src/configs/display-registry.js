/**
 * Import of the standard type-based ui5 type renderer components
 * If you want to overwrite standard components, you have to change from all-in-one import to single import.
 *
 * Components that are not present in the standard registry can simply be added below.
 */
import '@furo/ui5/src/typerenderer/registry.js';
